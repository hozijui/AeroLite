<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cdipcc.aerolite.server.dao.UserDao">

    <resultMap type="cn.cdipcc.aerolite.server.entity.User" id="UserMap">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="nickname" column="nickname" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="modifyTime" column="modify_time" jdbcType="TIMESTAMP"/>
        <result property="lastLogin" column="last_login" jdbcType="TIMESTAMP"/>
        <result property="enabled" column="enabled" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          id, username, password, nickname, create_time, modify_time, last_login, enabled
        from sys_user
        where id = #{id}
    </select>

    <select id="queryByUsername" resultMap="UserMap">
        select
            id, username, password, nickname, create_time, modify_time, last_login, enabled
        from sys_user
        where username = #{username}
    </select>

    <!--查询指定行数据-->
    <select id="queryAll" resultMap="UserMap">
        select
          id, username, password, nickname, create_time, modify_time, last_login, enabled
        from sys_user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="nickname != null and nickname != ''">
                and nickname = #{nickname}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="modifyTime != null">
                and modify_time = #{modifyTime}
            </if>
            <if test="lastLogin != null">
                and last_login = #{lastLogin}
            </if>
            <if test="enabled != null">
                and enabled = #{enabled}
            </if>
        </where>
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sys_user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="nickname != null and nickname != ''">
                and nickname = #{nickname}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="modifyTime != null">
                and modify_time = #{modifyTime}
            </if>
            <if test="lastLogin != null">
                and last_login = #{lastLogin}
            </if>
            <if test="enabled != null">
                and enabled = #{enabled}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sys_user(username, password, nickname, create_time, modify_time, last_login, enabled)
        values (#{username}, #{password}, #{nickname}, #{createTime}, #{modifyTime}, #{lastLogin}, #{enabled})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_user(username, password, nickname, create_time, modify_time, last_login, enabled)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.username}, #{entity.password}, #{entity.nickname}, #{entity.createTime}, #{entity.modifyTime}, #{entity.lastLogin}, #{entity.enabled})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_user(username, password, nickname, create_time, modify_time, last_login, enabled)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.nickname}, #{entity.createTime}, #{entity.modifyTime}, #{entity.lastLogin}, #{entity.enabled})
        </foreach>
        on duplicate key update
        username = values(username),
        password = values(password),
        nickname = values(nickname),
        create_time = values(create_time),
        modify_time = values(modify_time),
        last_login = values(last_login),
        enabled = values(enabled)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sys_user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="modifyTime != null">
                modify_time = #{modifyTime},
            </if>
            <if test="lastLogin != null">
                last_login = #{lastLogin},
            </if>
            <if test="enabled != null">
                enabled = #{enabled},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from sys_user where id = #{id}
    </delete>

    <resultMap id="UserRoleMap" type="cn.cdipcc.aerolite.server.dto.UserRole">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="nickname" column="nickname" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="modifyTime" column="modify_time" jdbcType="TIMESTAMP"/>
        <result property="lastLogin" column="last_login" jdbcType="TIMESTAMP"/>
        <result property="enabled" column="enabled" jdbcType="INTEGER"/>
        <collection javaType="java.util.ArrayList" ofType="cn.cdipcc.aerolite.server.entity.Role" property="roles">
            <id property="id" column="r_id" jdbcType="BIGINT"/>
            <result property="role" column="role" jdbcType="VARCHAR"/>
            <result property="name" column="name" jdbcType="VARCHAR"/>
            <result property="description" column="description" jdbcType="VARCHAR"/>
            <result property="createTime" column="r_create_time" jdbcType="TIMESTAMP"/>
            <result property="modifyTime" column="r_modify_time" jdbcType="TIMESTAMP"/>
            <result property="enabled" column="r_enabled" jdbcType="INTEGER"/>
        </collection>
    </resultMap>

    <select id="getRolesByUsername" resultMap="UserRoleMap">
        select
            u.id,
            u.username,
            u.password,
            u.nickname,
            u.create_time,
            u.modify_time,
            u.last_login,
            u.enabled,

            r.id as r_id,
            r.role,
            r.name,
            r.description,
            r.create_time as r_create_time,
            r.modify_time as r_modify_time,
            r.enabled as r_enabled
        from sys_user as u
        left join sys_user_role as ur on u.id = ur.user_id
        left join sys_role as r on ur.role_id = r.id
        where u.username = #{username};
    </select>

    <resultMap id="UserInfoMap" type="cn.cdipcc.aerolite.server.dto.UserInfo">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="nickname" column="nickname" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="modifyTime" column="modify_time" jdbcType="TIMESTAMP"/>
        <result property="lastLogin" column="last_login" jdbcType="TIMESTAMP"/>
        <result property="enabled" column="enabled" jdbcType="INTEGER"/>
        <collection javaType="java.util.Set" ofType="java.lang.String" property="roles">
            <result property="role" column="role" jdbcType="VARCHAR"/>
        </collection>
        <collection javaType="java.util.Set" ofType="java.lang.String" property="permissions">
            <result property="permission" column="permission" jdbcType="VARCHAR"/>
        </collection>
    </resultMap>

    <select id="getUserInfo" resultMap="UserInfoMap">
        select
            u.id,
            u.username,
            u.nickname,
            u.create_time,
            u.modify_time,
            u.last_login,
            u.enabled,
            r.role,
            p.permission
        from sys_user as u
        left join sys_user_role as ur on u.id = ur.user_id
        left join sys_role as r on ur.role_id = r.id
        left join sys_role_permission as rp on r.id = rp.role_id
        left join sys_permission as p on rp.permission_id = p.id
        where u.username = #{username};
    </select>

</mapper>

